import SwiftUI

struct HomeView: View {
    var body: some View {
        ZStack {
            Color.black.ignoresSafeArea()
            Text("HomeView")
        }
    }
}

#Preview {
    HomeView()
} 