import SwiftUI

struct MoodboardEditorView: View {
    var body: some View {
        Text("MoodboardEditorView")
            .toolbar(.hidden, for: .tabBar)
    }
}

#Preview {
    MoodboardEditorView()
} 